extends layout

block content
  .container
    .form
      label
        | Rate X:
        input#xRate(value='16.45', type='number', min=1)
      label
        | Rate Y:
        input#yRate(value='18.9', type='number', min=1)
      label
        | Tolerance:
        input#tolerance(value='.02', type='number', step='.001', min='.01', max='.02')
      button#compute.btn Submit
    #errorDisplay
